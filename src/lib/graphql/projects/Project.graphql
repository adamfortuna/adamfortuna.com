query Project($slug:String!) {
  projects(filters:{slug: {eq: $slug} }) {
    data {
      attributes {
        title
        slug
        description

        icon {
					data {
            attributes {
              url
              caption
            }
          }
        }
      }
    }
  }
}